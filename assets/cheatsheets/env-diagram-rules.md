# 环境图速查表

> 环境图是CS61A的核心概念，必须熟练掌握。

## 基本规则

1. **函数调用时**：创建新帧，绑定实参到形参
2. **函数定义时**：创建函数对象，记录当前环境为父环境
3. **赋值时**：在当前帧中查找/创建变量
4. **查找时**：先在当前帧，再沿父环境链向上

## 常见陷阱

- 可变对象 vs 不可变对象的绑定区别
- 高阶函数中函数对象的环境绑定
- 递归调用时的帧栈增长

---

待补充个人整理的详细规则...
